# formalization.yaml — 证明仓署名声明（硬要求：account ↔ author 须与仓库属主一致）
# Proof-repository authorship declaration.
# Hard requirement: account ↔ author must match the repository owner (wangweiqi388).

formalizer:
  account: wangweiqi388
  author: "王玮琪 (WangWeiqi)"

assistants:
  - name: WorkBuddy
    role: "AI assistant (drafting / tooling support)"
